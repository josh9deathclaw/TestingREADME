{
"project_title": "DFIR Simulation Trainer",
"project_description": "A full-stack training platform built with Express and React for creating digital forensics and incident response scenarios. It supports class management, browser-based investigation labs, virtual machine orchestration, and instructor grading workflows.",
"project_installation": "1. Install server dependencies: cd server && npm install\n2. Install web app dependencies: cd web_app && npm install\n3. Create server .env file with required variables. TODO: Add Details\n4. Initialise PostgreSQL database using dfir_dump.sql or server/src/db scripts. TODO: Add Details",
"usage": "Start backend API: cd server && npm start\nStart frontend interface: cd web_app && npm start\nHealth check available at /api/health",
"readme_markdown": "# DFIR Simulation Trainer\n\n## Description\nThe platform enables instructors to author investigative scenarios and organize learners into classes. Students interact with evidence, narrative phases, and a remote forensic desktop while instructors track attempts and provide scores.\n\n## Installation\nInstall dependencies for the API and the interface separately. From the repository root run cd server &amp;&amp; npm install and cd web_app &amp;&amp; npm install. Create a .env file for the server to define connection settings and secrets. TODO: Add Details for required variables. Initialise the PostgreSQL database using dfir_dump.sql or the scripts in server/src/db. TODO: Add Details for database setup.\n\n## Usage\nStart the API with:\nbash\ncd server\nnpm start\n\nStart the interface with:\nbash\ncd web_app\nnpm start\n\nThe API exposes a health endpoint at /api/health and versioned routes under /api.\n\n## Dependency Summary\n### server\n- bcryptjs\n- cors\n- dockerode\n- dotenv\n- express\n- jsonwebtoken\n- multer\n- nodemailer\n- pg\n- reactflow\n- nodemon\n\n### web_app\n- @testing-library/dom\n- @testing-library/jest-dom\n- @testing-library/react\n- @testing-library/user-event\n- react\n- react-dom\n- react-router-dom\n- react-scripts\n- reactflow\n- web-vitals\n\n## Containers\nA Dockerfile is provided at server/docker/forensic-desktop/Dockerfile for the forensic desktop environment. Build and run containers as required for VM features. TODO: Add Details\n\n## Authors\nJoshua Lillington-Moore"
}


